<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="com.eet.pma.maria.shoppinglist.ShoppingListActivity">

    <ListView
        android:id="@+id/llista"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_above="@+id/edit_box"
        android:layout_alignParentTop="true" />

    <RelativeLayout
        android:id="@+id/edit_box"
        android:background="#1a000000"
        android:layout_width="match_parent"
        android:layout_height="?android:listPreferredItemHeight"
        android:layout_alignParentBottom="true"
        android:layout_alignParentStart="true"
        android:paddingBottom="5dp"
        >

        <Button
            android:id="@+id/btn_afegir"
            style="@style/Widget.AppCompat.Button.Colored"
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            android:text="+"
            android:textSize="24sp"
            android:layout_marginEnd="10dp"
            android:layout_alignParentEnd="true"
            android:layout_alignBottom="@+id/edit_item"
            android:onClick="afegir"/>

        <EditText
            android:id="@+id/edit_item"
            android:layout_width="265dp"
            android:layout_height="match_parent"
            android:layout_centerVertical="true"
            android:ems="10"
            android:inputType="text"
            android:layout_alignParentStart="true" />
    </RelativeLayout>

</RelativeLayout>
